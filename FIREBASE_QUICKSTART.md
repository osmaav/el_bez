# 🔥 Настройка Firebase (КРАТКАЯ ИНСТРУКЦИЯ)

## ⚡ Быстрый старт

### 1. Создайте проект Firebase

1. Перейдите на https://console.firebase.google.com/
2. Нажмите **"Create project"**
3. Введите название: `el-bez-auth`
4. Нажмите **"Continue"** → **"Create project"**

### 2. Включите Authentication

1. В меню слева: **Build** → **Authentication**
2. Нажмите **"Get started"**
3. Включите **Email/Password**:
   - Нажмите **"Email/Password"**
   - Переключите в **"Enable"**
   - Нажмите **"Save"**

### 3. Создайте веб-приложение

1. В меню: **Project settings** (шестерёнка)
2. Внизу: **"Your apps"** → иконка **Web** (`</>`)
3. Введите название: `el-bez-web`
4. Скопируйте `firebaseConfig`

### 4. Заполните .env.local

1. Откройте файл `.env.local` в корне проекта `app/`
2. Вставьте значения из Firebase:

```env
REACT_APP_FIREBASE_API_KEY=AIzaSy...
REACT_APP_FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com
REACT_APP_FIREBASE_PROJECT_ID=your-project-id
REACT_APP_FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com
REACT_APP_FIREBASE_MESSAGING_SENDER_ID=123456789
REACT_APP_FIREBASE_APP_ID=1:123456789:web:abc123
```

### 5. Проверьте работу

1. Запустите приложение:
```bash
cd app
npm run dev
```

2. Откройте: http://localhost:5173/register

3. Заполните форму регистрации

---

## 🎯 Mock-режим (для разработки)

**Пока Firebase не настроен**, приложение работает в **mock-режиме**:

- ✅ Регистрация работает (данные в localStorage)
- ✅ Вход работает
- ✅ OAuth кнопки работают (mock)
- ❌ Данные не сохраняются на сервере
- ❌ Email verification не работает

**Индикатор mock-режима:**
- Откройте консоль браузера (F12)
- Вы увидите: `⚠️ Firebase не настроен. Работа в mock-режиме.`

---

## 📋 Поля для регистрации

| Поле | Обязательно | Пример |
|------|-------------|--------|
| Фамилия | ✅ | Иванов |
| Имя | ✅ | Иван |
| Отчество | ❌ | Иванович |
| Дата рождения | ✅ | 1990-01-01 |
| Место работы | ✅ | ООО «Ромашка» |
| Должность | ✅ | Инженер |
| Email | ✅ | test@example.com |
| Пароль | ✅ | password123 |

---

## 🔒 Firestore правила (обязательно!)

После настройки Firebase откройте **Firestore Database** → **Rules**:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
```

---

## 🆘 Troubleshooting

### Ошибка: "Invalid API key"

**Решение:** Проверьте `.env.local` — убедитесь, что API ключ скопирован без ошибок

### Ошибка: "Email already in use"

**Решение:** Пользователь с таким email уже существует. Используйте другой email или войдите.

### Mock-режим не работает

**Решение:**
1. Очистите localStorage: `localStorage.clear()`
2. Перезагрузите страницу
3. Проверьте консоль на ошибки

---

## 📚 Полная инструкция

См. файл `FIREBASE_SETUP.md` для подробной настройки всех провайдеров (Apple, Yandex, Telegram).
