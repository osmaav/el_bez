# 🧪 Тестовые компоненты

Эта директория содержит временные тестовые компоненты для отладки.

## Компоненты

### FirebaseTestPage.tsx

Страница для тестирования подключения к Firebase с подробными логами.

**URL:** http://localhost:5173/firebase-test

**Использование:**

1. Добавьте маршрут в `App.tsx`:
   ```tsx
   import { FirebaseTestPage } from '@/tests/FirebaseTestPage';
   
   // В Routes:
   <Route path="/firebase-test" element={<FirebaseTestPage />} />
   ```

2. Откройте браузер
3. Нажмите "🧪 Запустить тесты"

### EnvDebug.tsx

Компонент для отладки переменных окружения.

Используется внутри `FirebaseTestPage.tsx`.

## Функционал FirebaseTestPage

### Тесты

1. ✅ Проверка конфигурации Firebase
2. ✅ Проверка Firebase App
3. ✅ Проверка Firebase Auth
4. ✅ Проверка Firebase Firestore
5. ✅ Регистрация тестового пользователя
6. ✅ Проверка localStorage

### Логи

- Детальные логи каждого шага
- Цветовая индикация (инфо/успех/ошибка/предупреждение)
- Временные метки
- Детали ошибок с кодами и stack trace

### Управление

- **🧪 Запустить тесты** — начать тестирование
- **🗑️ Очистить** — очистить логи и результаты
- **📥 Экспорт** — сохранить логи в файл

## Документация

См. `FIREBASE_TEST_PAGE.md` в корне проекта для подробной документации.

## Удаление

После завершения отладки удалите эту директорию:

```bash
rm -rf app/tests
```

И удалите маршрут из `App.tsx`:

```tsx
// Удалить эту строку:
<Route path="/firebase-test" element={<FirebaseTestPage />} />
```

---

**Дата создания:** 25 февраля 2026 г.
**Статус:** Временные компоненты для отладки
