# Релиз 2026.03.13 — Улучшение фильтра вопросов

**Дата релиза:** 13 марта 2026 г.  
**Версия:** 2026.03.13  
**Ветка:** `master`

---

## 📋 Обзор изменений

Этот релиз посвящен улучшению работы фильтра вопросов в режиме обучения. Основные изменения направлены на корректное сохранение и восстановление настроек фильтра между переключениями разделов и перезагрузкой страницы.

---

## ✨ Новые возможности

### Тестирование
- ✅ Добавлены тесты для `questionFilterService` (24 теста)
  - Тесты сохранения/загрузки настроек
  - Тесты переключения фильтров
  - Тесты фильтрации вопросов
  - Тесты работы со скрытыми вопросами

---

## 🐛 Исправления

### Фильтр вопросов
- **Исправлено:** Настройки фильтра теперь корректно сохраняются в localStorage
- **Исправлено:** Настройки восстанавливаются при перезагрузке страницы
- **Исправлено:** Настройки сохраняются для каждого раздела отдельно (1256-19 / 1258-20)
- **Исправлено:** Кнопка "Сбросить фильтры" теперь корректно закрывает модальное окно
- **Исправлено:** Список вопросов в модальном окне фильтруется при переключении "Исключить известные/слабые"
- **Исправлено:** Кнопка фильтра окрашивается при активном фильтре
- **Исправлено:** Переход на первую страницу при применении фильтра

### Технические исправления
- **Исправлено:** `navigation.resetPage()` заменён на `navigation.goToPage(1)`
- **Исправлено:** Добавлена проверка на изменение значений перед сохранением в localStorage
- **Исправлено:** Убрано дублирующее сохранение настроек

---

## 🔧 Технические изменения

### Обновленные файлы

#### Хуки
- `src/sections/learning/hooks/useQuestionFilter.ts`
  - Добавлены состояния `excludeKnown`, `excludeWeak`
  - Добавлен `useEffect` для сохранения настроек при изменении
  - Добавлен флаг `isInitialMount` для пропуска сохранения при первой загрузке
  - Добавлен `prevSettings` для сравнения предыдущих значений

#### Компоненты
- `src/components/ui/FilterModal.tsx`
  - Добавлен `filteredQuestionsList` для фильтрации списка вопросов
  - Обновлен интерфейс `onApply` с передачей `hiddenQuestionIds`
  - Добавлен callback `onReset`

#### Разделы
- `src/sections/LearningSection.tsx`
  - Заменён импорт `useLearningFilter` на `useQuestionFilter`
  - Обновлены handler'ы: `handleFilterApply`, `handleResetFilter`
  - Добавлено состояние `isFilterModalOpen`

#### Сервисы
- `src/services/questionFilterService.ts`
  - Без изменений (используется существующий сервис)

#### Тесты
- `src/services/__tests__/questionFilterService.test.ts` (новый файл, 24 теста)
- `src/services/__tests__/ExportPdf.test.ts` (обновлён, 15 тестов)

---

## 📊 Статистика

| Параметр | Значение |
|----------|----------|
| Всего тестов | 461 |
| Изменено файлов | 8 |
| Добавлено тестов | 24 |
| Исправлено ошибок | 7 |

---

## 🧪 Тестирование

Все тесты проходят успешно:

```bash
npm run test

# Результат:
# Test Files  40 passed (40)
# Tests       461 passed (461)
```

### Ключевые сценарии тестирования

1. **Сохранение настроек при изменении**
   - Скрытие вопросов вручную
   - Включение "Исключить известные"
   - Включение "Исключить слабые"

2. **Восстановление после перезагрузки**
   - Настройки загружаются из localStorage
   - Фильтры применяются автоматически

3. **Переключение между разделами**
   - Настройки для 1256-19 и 1258-20 хранятся отдельно
   - При переключении загружаются соответствующие настройки

4. **Сброс фильтра**
   - Кнопка "Сбросить фильтры" очищает все настройки
   - Модальное окно закрывается
   - Кнопка фильтра перестает быть окрашенной

---

## 📝 Changelog

### Fix
- fix(filter): применение скрытия вопросов по кнопке Применить
- fix(filter): кнопка фильтра окрашивается при ручном скрытии вопросов
- fix(filter): кнопка Сбросить фильтры теперь работает корректно
- fix(filter): корректное применение фильтра при снятии скрытия вопросов
- fix(filter): сохранение настроек фильтра при переключении разделов
- fix(filter): корректное сохранение настроек при перезагрузке
- fix(LearningSection): использование useQuestionFilter вместо useLearningFilter
- fix(LearningSection): добавлен импорт useLearningNavigation
- fix(LearningSection): заменено filter.filteredQuestions на filteredQuestions

### Test
- test(pdf): добавлены тесты для 4 столбцов и красных ответов
- test: добавлены тесты для questionFilterService (24 теста)

### Style
- style: синие прогресс-бары и акценты
- style(pdf): унификация оформления таблицы обучения

### Refactor
- refactor(filter): убрано логирование из кода

---

## ✅ Чек-лист релиза

- [x] Все тесты проходят
- [x] Код отформатирован
- [x] Изменения закоммичены
- [x] Изменения отправлены на GitHub
- [x] Документация обновлена
- [x] Changelog составлен

---

## 🚀 Деплой

Деплой на GitHub Pages выполняется автоматически при пуше в ветку `master`.

Workflow: `.github/workflows/deploy.yml`

---

## 📞 Контакты

- **Репозиторий:** https://github.com/osmaav/el_bez
- **Демо:** https://osmaav.github.io/el_bez/

---

*Релиз подготовлен: 13 марта 2026 г.*
