# el-bez — Проект веб-приложения для подготовки к экзаменам по электробезопасности

## 📋 Обзор проекта

**el-bez** — современное React-приложение для самостоятельной подготовки к проверке знаний по электробезопасности (тесты Ростехнадзора).

### Основные характеристики

- **Тип:** Веб-приложение (Single Page Application)
- **Назначение:** Подготовка к экзаменам по электробезопасности
- **Целевая аудитория:** Специалисты по электробезопасности (3 и 4 группа допуска)

### Поддерживаемые разделы

| Код | Название | Группа | Вопросов | Билетов |
|-----|----------|--------|----------|---------|
| **ЭБ 1256.19** | Тесты по электробезопасности | 3 группа до 1000 В | 250 | 25 |
| **ЭБ 1258.20** | Тесты по электробезопасности | 4 группа до 1000 В | 309 | 31 |

### Функциональные возможности

- **Теория** — справочные материалы по электробезопасности
- **Обучение** — изучение вопросов по 10 на странице с сохранением прогресса
- **Тренажёр** — случайная выборка вопросов (20 или 50) с мгновенной проверкой
- **Экзамен** — имитация реального тестирования (билеты по 10 вопросов)
- **Регистрация/Авторизация** — Firebase Auth с mock-режимом для разработки

---

## 🛠 Технологический стек

### Frontend

- **React 19** — UI библиотека
- **TypeScript 5.9** — типизация
- **Vite 7.2** — сборщик и dev-сервер
- **Tailwind CSS 3.4** — стилизация
- **shadcn/ui** — UI компоненты (Radix UI为基础)
- **React Router 7** — маршрутизация

### State Management & Storage

- **React Context API** — глобальное состояние
- **localStorage** — сохранение прогресса пользователя
- **react-cookie** — работа с cookies

### Backend & Auth

- **Firebase Auth** — аутентификация (email/password, OAuth)
- **Firebase Firestore** — хранение профилей пользователей
- **Mock-режим** — для разработки без настроенного Firebase

### Ключевые зависимости

```json
{
  "react": "^19.2.0",
  "react-dom": "^19.2.0",
  "typescript": "~5.9.3",
  "vite": "^7.2.4",
  "tailwindcss": "^3.4.19",
  "@radix-ui/*": "UI компоненты",
  "react-hook-form": "^7.70.0",
  "lucide-react": "^0.562.0",
  "firebase": "^12.9.0",
  "react-router-dom": "^7.13.1"
}
```

---

## 📁 Структура проекта

```
el-bez/app/
├── .github/workflows/         # CI/CD конфигурация
├── docs/                      # Продакшен-сборка (GitHub Pages)
├── node_modules/              # Зависимости
├── src/
│   ├── components/
│   │   ├── ui/               # UI компоненты (shadcn/ui)
│   │   ├── LoginModal.tsx    # Модальное окно входа
│   │   ├── Navigation.tsx    # Навигация с переключателем разделов
│   │   └── RegisterForm.tsx  # Форма регистрации
│   ├── context/
│   │   └── AppContext.tsx    # Глобальное состояние приложения
│   ├── data/
│   │   ├── questions-1256-19.json  # База вопросов (3 группа)
│   │   ├── questions-1258-20.json  # База вопросов (4 группа)
│   │   └── questions.d.ts    # TypeScript типы для JSON
│   ├── hooks/                # Кастомные React-хуки
│   ├── lib/
│   │   └── firebase.ts       # Конфигурация Firebase
│   ├── pages/
│   │   └── RegisterPage.tsx  # Страница регистрации
│   ├── sections/
│   │   ├── LearningSection.tsx  # Режим обучения
│   │   ├── TheorySection.tsx    # Теория
│   │   ├── TrainerSection.tsx   # Тренажёр
│   │   └── ExamSection.tsx      # Экзамен
│   ├── services/
│   │   └── authService.ts    # Сервис аутентификации
│   ├── tests/
│   │   └── FirebaseTestPage.tsx  # Тестовая страница Firebase
│   ├── types/
│   │   ├── auth.ts           # Типы для аутентификации
│   │   └── index.ts          # Основные типы приложения
│   ├── App.tsx               # Корневой компонент
│   ├── App.css               # Стили приложения
│   ├── index.css             # Глобальные стили
│   └── main.tsx              # Точка входа
├── public/                    # Статические файлы
├── package.json
├── tsconfig.json
├── tsconfig.app.json
├── tsconfig.node.json
├── vite.config.ts
├── tailwind.config.js
└── postcss.config.js
```

---

## 🚀 Запуск и разработка

### Требования

- **Node.js 20+**
- **npm 10+**

### Установка зависимостей

```bash
cd /Users/osmaav/Downloads/el-bez/app
npm install
```

### Запуск dev-сервера

```bash
npm run dev
```

Приложение будет доступно по адресу: `http://localhost:5173`

### Сборка для продакшена

```bash
npm run build
```

Результат сохраняется в директорию `docs/` (настроено для GitHub Pages).

### Предпросмотр продакшен-сборки

```bash
npm run preview
```

### Линтинг

```bash
npm run lint
```

---

## 🔧 Конфигурация

### Переменные окружения

Для работы Firebase создайте файл `.env.local` в корне проекта:

```env
VITE_FIREBASE_API_KEY=your_api_key
VITE_FIREBASE_AUTH_DOMAIN=your_project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your_project_id
VITE_FIREBASE_STORAGE_BUCKET=your_project.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
VITE_FIREBASE_APP_ID=your_app_id
VITE_FIREBASE_MEASUREMENT_ID=your_measurement_id
```

**Важно:** Без настройки Firebase приложение работает в mock-режиме (данные сохраняются в localStorage).

### Настройки сборщиков

| Файл | Описание |
|------|----------|
| `vite.config.ts` | Настройки Vite, алиас `@` → `./src` |
| `tsconfig.json` | Конфигурация TypeScript |
| `tailwind.config.js` | Тема Tailwind CSS |
| `eslint.config.js` | Правила ESLint |

---

## 📦 Формат данных

### Структура вопроса (JSON)

```json
{
  "metadata": {
    "name": "ЭБ 1256.19",
    "description": "Тесты по электробезопасности 3 группа до 1000 В",
    "totalQuestions": 250,
    "totalTickets": 25,
    "version": "2026.02"
  },
  "questions": [
    {
      "id": 1,
      "ticket": 1,
      "question": "Текст вопроса?",
      "answers": [
        "Вариант 1",
        "Вариант 2",
        "Вариант 3",
        "Вариант 4"
      ],
      "correct": 2,
      "link": "Источник (ПУЭ, Приказ и т.д.)"
    }
  ]
}
```

### TypeScript типы

```typescript
// src/types/index.ts
interface Question {
  id: number;
  ticket: number;
  text: string;
  options: string[];
  correct_index: number;
  link?: string;
}

interface Ticket {
  id: number;
  questions: Question[];
}

type SectionType = '1256-19' | '1258-20';

interface SectionInfo {
  id: SectionType;
  name: string;
  description: string;
  totalQuestions: number;
  totalTickets: number;
}

type PageType = 'learning' | 'theory' | 'trainer' | 'exam';
```

---

## 🎯 Ключевые компоненты приложения

### AppContext (`src/context/AppContext.tsx`)

Глобальное состояние приложения:

- Управление текущим разделом (1256-19 / 1258-20)
- Загрузка вопросов из соответствующего JSON-файла
- Состояние для тренажёра и экзамена
- Сохранение прогресса в localStorage

**Хуки:**
- `useApp()` — доступ к контексту из любого компонента

### Navigation (`src/components/Navigation.tsx`)

Компонент навигации:

- Переключатель разделов (выпадающий список)
- Навигация между страницами (Теория, Обучение, Тренажёр, Экзамен)
- Отображение текущей группы допуска
- Кнопка выхода

### AuthService (`src/services/authService.ts`)

Сервис аутентификации:

- Регистрация через email/password
- Вход через email/password
- OAuth провайдеры (Apple, Yandex)
- Mock-режим для разработки
- Подписка на изменения состояния

**Функции:**
- `registerUser(userData)` — регистрация
- `loginUser(userData)` — вход
- `logoutUser()` — выход
- `getUserProfile(uid)` — получение профиля
- `updateUserProfile(uid, updates)` — обновление профиля
- `onAuthChange(callback)` — подписка на изменения
- `validateRegisterData(data)` — валидация данных

### Firebase (`src/lib/firebase.ts`)

Модуль инициализации Firebase:

- Проверка наличия конфигурации
- Инициализация с реальными данными или mock-режим
- Экспорт объектов `auth`, `db`, `app`

**Функции:**
- `isFirebaseReady()` — проверка готовности Firebase

### Sections

| Компонент | Описание |
|-----------|----------|
| `LearningSection` | Обучение по 10 вопросов на странице |
| `TheorySection` | Теоретические материалы |
| `TrainerSection` | Тренажёр со случайной выборкой |
| `ExamSection` | Симуляция экзамена по билетам |

---

## 📊 Статистика проекта

### Вопросы

- **Всего вопросов:** 559 (250 + 309)
- **Билетов:** 56 (25 + 31)
- **Источники:** тесты Ростехнадзора

### Технические метрики

- **Компонентов UI:** 40+ (shadcn/ui)
- **Размер сборки:** ~960 KB (minified)
- **Время сборки:** ~2.5 секунды

---

## 🧪 Практики разработки

### Кодстайл

- **TypeScript** для типизации
- **ESLint** для линтинга
- **Prettier** (через ESLint) для форматирования

### Тестирование

В проекте есть тестовая страница Firebase (`src/tests/FirebaseTestPage.tsx`).

Рекомендуется добавить:
- Unit-тесты для компонентов (Vitest / Jest)
- E2E-тесты (Playwright / Cypress)

### Вклад в проект

1. Создать fork репозитория
2. Создать ветку (`git checkout -b feature/...`)
3. Внести изменения
4. Закоммитить (`git commit -m '...'`)
5. Отправить в fork (`git push origin feature/...`)
6. Создать Pull Request

---

## 📝 Дополнительные файлы

| Файл | Описание |
|------|----------|
| `CHANGELOG.md` | История изменений |
| `CONTRIBUTING.md` | Руководство для контрибьюторов |
| `LICENSE` | Лицензия MIT |
| `RELEASE.md` | Процесс релиза |
| `components.json` | Конфигурация shadcn/ui |
| `FIREBASE_SETUP.md` | Инструкция по настройке Firebase |
| `FIREBASE_QUICKSTART.md` | Быстрый старт с Firebase |
| `REGISTRATION_COMPLETE.md` | Документация по регистрации |

---

## ⚠️ Важные замечания

### Для разработки

- Все данные пользователей хранятся в localStorage (mock-режим) или Firebase
- Переменные окружения начинаются с `VITE_` (требование Vite)
- Сборка настроена на деплой в `docs/` для GitHub Pages

### Для Firebase

- Без настройки `.env.local` приложение работает в mock-режиме
- Mock-данные сохраняются в `localStorage`
- Для продакшена требуется настройка Firebase Console

### Для пользователей

- Приложение для образовательных целей
- Вопросы из открытых источников
- Для официальной проверки знаний обращаться в центры Ростехнадзора

---

## 🔗 Ссылки

- **Репозиторий:** https://github.com/osmaav/el_bez
- **Демо:** (развёрнуто на GitHub Pages)
- **Автор:** Андрей Осьмаков

---

*Последнее обновление: 27 февраля 2026 г.*
